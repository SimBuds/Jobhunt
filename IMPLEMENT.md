# IMPLEMENT.md — Execution engine

Pillar 4 of the documentation architecture defined in [AGENTS.md](AGENTS.md).
This file is the granular, phase-by-phase breakdown of in-flight work. Agents
**read** this at the start of every phase (Phase 1 and Phase 3 of the Workflow
Contract) and **write** to it as part of the Definition of Done — checking off
completed phases and logging deferred work as new phases. Never leave a plan in
conversational context alone; it lives here.

## Current state

**No active plan.**

Standing context that isn't obvious from the code:
- `kb/profile/` is gitignored PII (regenerated by `jobhunt convert-resume`).
  Hand-edits to `verified.json` / `skills.md` are authoritative at runtime but
  are dropped by a `convert-resume` re-run unless also encoded in `Resume.docx`.
- Skill honesty tiers: **Core** (paid production) / **Projects**
  (`skills_projects`, school+personal depth — currently Astro) / **Familiar**
  (light/academic — Java, Spring Boot, Angular, MCP, Agile, Headless, Figma).
- **React umbrella:** verified Core skill is `React (Redux, React Native)`;
  renders as plain "React" unless a JD names Redux/React Native (then surfaced,
  Core-grade). No standalone Redux item.
- `include_senior_roles=false` in `~/.config/jobhunt/config.toml` (Casey <3 YoE).

**Open interim item (no deadline):** the React-umbrella + Projects-tier skill
edits live only in the gitignored `verified.json`. When convenient, fold them
into `Resume.docx` (Core line → `React (Redux, React Native)`, drop standalone
Redux) so `convert-resume` stays consistent — the parser already supports both.

## Completed plans (archive)

Terse index; see git history for the full phase-by-phase detail that used to
live here.

- **React umbrella** (2026-05-28). Collapsed React/Redux/React Native into one
  Core umbrella skill `React (Redux, React Native)`; renders as "React" by
  default, surfaces Redux/React Native only when the JD names them. Rules in
  `tailor.md` / `tailoring-rules.md` / `Resume_Tailoring_Instructions.md` /
  AGENTS.md. Suite 693. *(A portfolio Projects-**section** feature was scoped
  then declined — Casey keeps resumes experience-forward; portfolio stays in
  the contact-line links.)*
- **Projects skill tier** (2026-05-28). Added the `skills_projects` honesty tier
  between Core and Familiar — wired through the fabrication guard + Familiar-only
  score cap, the one-page shrink ladder (`_PROJECTS_FLOOR`), prompt/policy docs,
  and `parse_docx` (with a `_PAGE_SAFETY_MARGIN` fix after a real `.docx`
  overflowed). Suite 692.
- **base qwen3.5:9b + app-owned options** (2026-05-28). Gateway pins
  `_DEFAULT_OPTIONS` (num_ctx 16384 + samplers) on every call so structured-task
  behavior is in-repo; switched default model to bare `qwen3.5:9b`. Root cause
  of prose-not-JSON was unset `OLLAMA_CONTEXT_LENGTH` → 4096 truncation.
- **Workday CXS GTA-targeted scan** (2026-05-28). Large Workday boards issue a
  deduped `_GTA_SEARCH_TERMS` union instead of a blank first-100 walk so GTA
  roles aren't buried. `_BLANK_SCAN_MAX=200`.
- **Post-audit fixes** (2026-05-27). `HARD_COVERAGE_FLOOR_PCT=50` escalates
  sub-50% coverage to `block`; `_OVERREACH_PATTERNS` in `cover_validate` catches
  framing-level capability claims; baseline `Resume.docx` summary rewrite.

## Active plan — Remove dormant Projects tier + docs audit

**Why:** `skills_projects` is now empty (Astro moved to Familiar; React Native
folded into the React umbrella). The tier's code, prompt rules, and policy text
are dead weight that actively mislead the LLM about a "Projects" category that
will never have data. Remove it; sweep the 4-pillar docs for staleness from this
session's churn. **Keep** the independently-valuable bits: the React umbrella,
`_PAGE_SAFETY_MARGIN` + wrap-aware coursework estimate, and the pre-existing
"Personal projects → cover-notes-only" guidance in `Resume_Tailoring_Instructions.md`.

### Phase 1 — Remove the `skills_projects` tier from code + tests

**Goal:** Strip the dead Projects-tier support from the guard, score cap, and
parser, keeping the page-fit safety margin and React umbrella.

**Files to touch:**
- `src/jobhunt/pipeline/tailor.py` — drop `"skills_projects"` from the `_enforce_no_fabrication` verified-skill tuple; remove `_PROJECTS_FLOOR` + the Projects trim step in `_shrink_to_one_page`.
- `src/jobhunt/pipeline/score.py` — drop `"skills_projects"` from `_all_matched_are_familiar`'s non-familiar key set.
- `src/jobhunt/resume/parse_docx.py` — remove `skills_projects` from `VerifiedFacts`, the `"Projects"` bucket, and the `## Projects` block in `write_kb_markdown`.
- `tests/test_tailor_invariants.py`, `tests/test_score_clamp.py`, `tests/test_parse_docx.py` — remove the 4 Projects-tier tests; keep umbrella + safety-margin tests.

**Functions to add/change:**
- `tailor._enforce_no_fabrication`, `tailor._shrink_to_one_page`, `score._all_matched_are_familiar`, `parse_docx.parse_baseline`/`write_kb_markdown` — change — remove tier handling.

**Reuse audit:** N/A — removal only.

**Verification:** (≤3 bullets)
- `rg skills_projects src/ tests/` returns nothing; `render_docx.py` comment may keep the historical mention.
- `jobhunt convert-resume` regenerates `verified.json` with no `skills_projects` key; umbrella + Familiar (incl. Astro) intact.
- `pytest -q` green.

**Status:** [ ] not started

### Phase 2 — Remove Projects-tier rules from prompts/policies

**Goal:** Delete the Projects-category instructions from the prompt/policy docs,
keeping the React umbrella and the cover-notes-only personal-projects guidance.

**Files to touch:**
- `kb/prompts/tailor.md` — remove rule 2a, the `skills_projects` clause in rule 6d, and the Projects-placement note in rule 10.
- `kb/policies/tailoring-rules.md` — remove the Projects-tier bullet.
- `Resume_Tailoring_Instructions.md` — remove the "Projects tier" skills subsection (~L46) + the rule-148 Projects line; add Astro to the Familiar list (~L52). Keep the lines 86-99 "Personal projects → cover notes" section.
- `AGENTS.md` — remove the Projects-tier note under the Familiar-only-fit cap; keep a one-line React-umbrella mention.

**Functions to add/change:** none (docs).

**Reuse audit:** N/A — removal/edit.

**Verification:** (≤3 bullets)
- `rg -i "projects tier|skills_projects|## Projects" kb/ AGENTS.md Resume_Tailoring_Instructions.md` returns only the kept personal-projects section.
- A live `apply` smoke still ships clean (no prompt references a missing tier).

**Status:** [ ] not started

### Phase 3 — Audit the 4-pillar docs for staleness

**Goal:** Reconcile AGENTS.md / PLAN.md / README.md with the current code after
this session's churn (qwen3.5:9b default, app-owned options, React umbrella).

**Files to touch:**
- `PLAN.md` — fix stale `qwen-custom`/`num_ctx`/model-default references to match the shipped state (gateway-pinned `num_ctx=16384`, bare `qwen3.5:9b`).
- `AGENTS.md`, `README.md` — spot-check the same terms for consistency; fix only confirmed drift (these were largely updated during the qwen plan).

**Functions to add/change:** none (docs).

**Reuse audit:** N/A.

**Verification:** (≤3 bullets)
- `qwen-custom`/`num_ctx` references across the 4 pillars agree with `gateway/client.py` + `config.py`.
- No doc claims a feature the code doesn't have (Projects tier, old model default).

**Status:** [ ] not started

## Phase template

Copy this block for each phase of an approved plan. One phase = one atomic,
revertable commit (see Phase-Sizing Rules in `AGENTS.md`).

```
### Phase <N> — <one-sentence goal, no "and">

**Goal:** <single declarative sentence>

**Files to touch:**
- path/to/file — what changes

**Functions to add/change:**
- module.function — add | change — what it does

**Reuse audit:** (per Reuse-First Rule in AGENTS.md)
- Search terms: `<grep/rg terms used>`
- Candidates found: <list, or "none">
- Why not reused: <reason per candidate>

**Verification:** (≤ 3 bullets)
- <how to prove it works — test name or manual E2E output>

**Status:** [ ] not started | [ ] in progress | [ ] done
```
